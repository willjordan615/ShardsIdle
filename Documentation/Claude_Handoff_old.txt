Session Handoff

Delivered This Session

gear-tooltip.js

Status effects in createSkillTooltip now display as bolded names (debuffs in #ffbbbb, buffs in #aaffbb) rather than raw type strings
_resolveStatusName() helper — looks up status ID in gameData.statuses, falls back to formatted ID string
Footer line appended to skill tooltips when any status is present: "Status effects listed in Field Codex → Statuses"
Item type line now shows "Two-Handed" badge: e.g. GREATSWORD • Two-Handed • Tier 3

index.html (Field Codex)

Statuses tab added to Field Codex alongside Skills / Combinations / Statistics / Combat
codex-pane-statuses — renders from gameData.statuses on first open, split into Debuffs and Buffs sections
switchCodexTab updated to include statuses; render is lazy and cached
Dual Wielding section added to Combat pane — documents 40% echo rate, 30% power, offhand weapon/proc behaviour, DAMAGE_SINGLE only restriction

combatEngine.js

_triggerDualWieldEcho(actor, skill, target, skillLevel) — new method
  Fires on DAMAGE_SINGLE hits only
  40% chance per hit
  Builds shallow actor proxy with offHand swapped into mainHand so calculateDamage resolves offhand weapon naturally
  Full damage calculation, then multiplied by 0.3 on output (not basePower scaling — full weapon damage is calculated then modulated)
  Offhand weapon procs fire independently via triggerWeaponProcs on the echo actor
  Returns 0 if offHand is empty or has no dmg1 (shields without damage values are safe)
Echo damage folds into hitDamage and totalDamage — appears in existing log messages as part of the hit total

inventory-system.js

Step 4a: equipping into offHand blocked if mainHand holds a two_handed item — showError fired, equip aborted
Step 4b: equipping a two_handed item into mainHand returns existing offHand to inventory and nulls the slot

backend/data/items.json

90 two-handed weapons appended across 6 classes: greatsword, greataxe, polearm, maul, bow, staff
All items carry two_handed: true, slot_id1: "mainHand", slot_id2: null
Tiers 0–8, 2 items per tier for tiers 0–5, 1 item per tier for tiers 6–8
Proc collision fixes applied across classes per design matrix recommendations

backend/data/skills.json

9 new proc skills appended — these statuses existed but had no proc skill wired:
  proc_slow, proc_freeze, proc_sleep, proc_confused, proc_ambition_falter,
  proc_taunt, proc_cursed, proc_bloodlust_buff, proc_fortify_buff
All follow standard proc skill structure: no cost, no delay, basePower 0, enemyUsable false

Outstanding / Notes

Smoketest on proc rates and AOE targeting still not run — carried from last session, still worth doing
Dual wield echo is not separately logged — folds into the main hit total. If you want echo hits identifiable in the log, that's a small addition once the mechanic confirms working
Two-handed weapons are in items.json but not yet in any loot tables or merchant pools — they won't drop or appear for purchase until wired into those
Enemy equip logic does not check two_handed — enemies are assigned weapons by ID directly, so no conflict there, but worth noting if enemies ever get two-handed weapons assigned
Spiked/damage-value shields would trigger dual wield echo — this is intentional by design, noted for awareness
Consumable pass and content work remain on the docket
