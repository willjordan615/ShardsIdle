Session Handoff

== DELIVERED THIS SESSION ==

backend/combatEngine.js
_childProcChance: now accepts character parameter. If character has the child skill at level >= 1, returns flat 0.05 proc rate instead of depth-based rate. Call site updated to pass character.

backend/data/tuning.json
No changes delivered — combatBonusPercent confirmed correct at 0.005 (not 0.05; developer caught a typo in their own request).

js/game-data.js
gameData.tuning = dataFromServer.tuning added to the server data assignment block. This was listed as delivered last session but was not in the zip. Without it, window.gameData.tuning is always undefined and all skill XP rates fall back to 50.0.

js/combat-system.js
displayCombatLog is now awaited. Previously fire-and-forget, which cleared _combatStartInFlight in finally while applyCombatRewards was still running async. This caused concurrent combat loops to interleave XP writes on the same character objects — the root cause of skills gaining XP they shouldn't have.

js/combat-log.js
- sleep() now measures against Date.now() from start rather than incrementing elapsed by 100ms. Survives background tab throttling — delays resolve correctly when tab returns.
- startCountdown() replaced counter-- with deadline = Date.now() + seconds * 1000. Countdown fires correctly after background tab.
- isDefeat = combatData.result === 'defeat' || 'loss' added before turn loop.
- Defeat halving: if (isDefeat) xpToAward *= 0.5 applied after both XP branches.
- Combat bonus gated to equipped skills only: equippedSkillIDs = new Set of character.skills.slice(0,2). Bonus only fires if equippedSkillIDs.has(skillID).
- Child proc XP rewrite: if child skill is level >= 1 and turn.isChildSkillProc, child gets no XP. XP is instead credited to the parent (turn.action.replacedSkillID) at the standard mastery rate for the parent's category, with defeat halving applied.
- Background tab catch-up system:
  - _watchedCombatMs: starts null. Set to actual cycle duration after each fully-watched combat (tab never hidden during that cycle).
  - _combatHiddenMidCycle: flag that prevents updating _watchedCombatMs if tab was hidden at any point.
  - _TAB_PROTECTION_MS = 5 minutes: tab-aways shorter than this are ignored entirely.
  - On visibilitychange show: if elapsed >= 5min and _watchedCombatMs is set, Math.floor(elapsed / _watchedCombatMs) added to _missedCombats.
  - displayCombatLog: sets combatSkipPlayback = true if _missedCombats > 0, decrements counter before rewards. combatSkipPlayback = false when counter hits 0.
  - sleep() resolves immediately if combatSkipPlayback is set.
- Avg Challenge Speed display: element #combatCycleSpeed added to media controls bar. Shows "∞ — Watch a full challenge to calibrate" until first fully-watched combat sets _watchedCombatMs, then shows "Avg Challenge: ~N Min".

js/inventory-system.js
- Two-handed tag added to gear modal item rows (equipped and inventory). def.two_handed ? '<span class="inv-two-handed-tag">Two-Handed</span>' appended to item name.
- Two-handed tag added to loadout panel slot display. twoHandedPrefix = '(2H)' prepended to item name when def.two_handed.

js/character-management.js
- Avatar background injected into #detailCharHeader on character detail screen.
- img.style.display = 'none' hides the img element (inline style overrides CSS display:none).
- Background set via backgroundImage, backgroundSize: auto 160%, backgroundRepeat: no-repeat on the bgEl div.
- Animation: detail-portrait-pan 14s ease-in-out infinite alternate applied directly to img via JS. translateY keyframe pans vertically within overflow:hidden container.

css/styles.css
- .health-bar-fill opacity reduced from 0.7 to 0.5.
- .mana-bar-fill and .stamina-bar-fill added as CSS classes (previously inline styles in combat-log.js), both opacity 0.5.
- .detail-char-header: position relative, overflow hidden, padding, border-radius. Avatar bg rules scoped to this container.
- @keyframes detail-portrait-pan: translateY from -20% to -45%.
- .inv-two-handed-tag: inline-block, color var(--text-secondary), opacity 0.7.

index.html
- #detailCharHeader wrapper div added around name/race/gold block on character detail screen.
- #combatCycleSpeed span added to the right of media controls in combat log nav.

== OUTSTANDING / UNRESOLVED ==

skillXPGains display scope — skillXPGains is declared outside the character loop and accumulates all party members' skill XP into one object, which is passed to the rewards modal. In a party with bots, the modal shows all party skill gains, not just the player's. This was identified as a display bug but not fixed this session — the underlying XP is applied correctly per character, only the modal aggregates across all.

Belt display bug — carried from last session. Reusable/quest items show non-decrementing quantity.

Countdown text on rewards modal — carried from last session. Unconfirmed for all result types.

== SMOKETESTS NEEDED ==

- game-data.js fix: confirm window.gameData.tuning is populated. Aim XP per use should drop dramatically (DAMAGE_SINGLE rate 2 at Lv.7 = ~0.91/use, not ~22.77).
- Combat bonus: confirm it only appears on the two equipped skills in the modal, not on discovered/unequipped skills.
- Child proc XP: confirm learned child skills (level >= 1) show no XP in modal. Confirm parent skill XP increases when child procs.
- Defeat halving: confirm skill XP on defeat is roughly half a comparable victory.
- Background tab catch-up: watch one full combat to calibrate _watchedCombatMs. Tab away for longer than 5 minutes. Return and confirm that many combats fast-forward before normal playback resumes.
- Two-handed tag: confirm (2H) appears in loadout panel and "Two-Handed" appears in gear modal for two-handed weapons.
- Avatar banner: confirm portrait animates on character detail screen. Confirm characters without avatars show no broken state.

== KNOWN ISSUES (carried) ==

Belt display bug — reusable/quest items show non-decrementing quantity.
Countdown text on rewards modal — unconfirmed for all result types.
