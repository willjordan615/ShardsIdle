Shards Idle — Session Handoff Date: 2026-04-06 (end of session)

What was done this session

How to Play codex tab — new codex-pane-guide added as first tab in the Field Codex. Opens automatically on first character creation via si_guide_pending localStorage flag, dismissed forever via "Got it — don't show again" button. All codex logic extracted from index.html into js/codex.js.
Generated bot snapshot fix — bots above level 12 are procedurally generated in-session and don't exist in bots.json. Idle collect was silently dropping them. Fix: _notifyIdleStart serializes full snapshots of bot_gen_* members, backend stores them in new idleGeneratedBots column, uses them as fallback at collect time. Files: js/combat-system.js, backend/routes/combat.js, backend/database.js.
Map touch fix — node clicks on mobile never fired due to touchstart calling e.preventDefault(). Tap detection added to touchend — under 8px movement fires mapOpenModal directly via elementFromPoint. File: index.html.
Merchant per-character — replaced global currentMerchant/currentMerchantStock with _merchantOffers map keyed by characterId. rollMerchantAppearance now takes characterId; call site in combat-log.js updated. Files: js/merchant.js, js/combat-log.js.
Skill web — reveal parents for gold — clicking a reachable node zooms to it and opens an HTML context menu. Shows "Reveal parents: Xg" — cost is 10^depth, halved if one parent already known. On purchase: gold deducted, skill ID added to character.revealedParents, connecting edge animates in as a gold dash-draw then settles solid. Buy skill option removed — reveal only, information not shortcuts. File: js/skill-tree.js.
Starter skills seeded at creation — all root skills (no parentSkills) are now added to a new character's skill array at level 0 with 0 XP. Players see their full starter web immediately on day one. The two chosen starting skills remain at level 1 as before. File: js/character-management.js.
What still needs doing

Existing character migration — existing characters don't have root starters seeded. A migration that adds missing root skills to any character that lacks them would make the web consistent across old and new characters. Small targeted change.
Skill web redesign — the SVG canvas is hard to follow. Agreed direction: vertical family-tree layout, seed at top, branches extend downward by depth, owned skills as trunk. Full redraw, meaningful scope. Save for a full session.
Idle simulation duration — idle/collect uses a hardcoded 2-minute cycle. The observed live combat time is never stored. Needs lastCombatDurationMs stored on character or session and wired into collect.
Known outstanding issues

js/map.js in repo is not loaded and out of sync with the inline map script in index.html. Dead file, harmless, but should be deleted.
renderChallenges() call in game-data.js line 267 exits silently — dead code.
Map label truncation at default zoom — names clip at 168px base width.
Idle simulation duration hardcoded at 2 minutes — noted above.
Files changed this session index.html, js/codex.js (new), js/combat-system.js, js/merchant.js, js/combat-log.js, js/skill-tree.js, js/character-management.js, backend/routes/combat.js, backend/database.js

