Session Handoff

Delivered This Session

skills.json

deploy_caltrops — damage effect replaced with slow + bleed (both AOE, duration 2, basePower 0)
deploy_trap_stun — stun chance dropped 0.6 → 0.25
restore_mana_major, restore_stam_minor, restore_stam_small — targets fixed from single_ally to self
use_health_potion, use_health_potion_minor — now scalesBy harmony; major bumped to 0.65
mend — parents changed from [first_aid, use_health_potion_minor] to [first_aid, heal_major]; magnitude 0.35 → 0.52
15 use_* wrapper skills removed entirely (use_health_potion, use_mana_potion, use_stamina_potion, etc.)
New skills added:
  heal_minor — flat 15hp, scalesBy flat, CONSUMABLE_HEALING, no parents
  heal_standard — flat 60hp, same pattern
  heal_major2 — flat 150hp, same pattern
  restore_stam — magnitude 0.45, RESTORATION
  restore_stam_major — magnitude 0.9, RESTORATION
  restore_mana_major2 — magnitude 0.9, RESTORATION
  purge — cleanse_debuffs self, CONSUMABLE_HEALING, no parents (antidote skill)
  elixir_of_vitality — heal 1.5 harmony + cleanse_debuffs, CONSUMABLE_HEALING
  use_ration / use_hearty_meal — removed (replaced by direct skill refs)

items.json

consumable_bomb_smoke removed (duplicate of smoke_bomb)
consumable_potion_health_minor/major, mana_minor/major, stamina_minor removed (duplicates with no challenge refs)
Legacy items fixed in place — tiers, goldValues, descriptions corrected
  health_potion_minor t1 $8, health_potion t3 $30, mana/stamina equivalents
New items added: stamina_potion (t3), health_potion_major (t5), mana_potion_major (t5),
  stamina_potion_major (t5), antidote (t2), elixir_vitality (t6), food_hearty_meal (t2)
All consumable skillID refs now point to real skills — no use_* wrappers remain
  health_potion_minor → heal_minor
  health_potion → heal_standard
  health_potion_major → heal_major2
  mana_potion_minor → restore_mana_minor
  mana_potion → restore_mana_major
  mana_potion_major → restore_mana_major2
  stamina_potion_minor → restore_stam_minor (second_wind parent — do not change)
  stamina_potion → restore_stam
  stamina_potion_major → restore_stam_major
  antidote → purge
  elixir_vitality → elixir_of_vitality
  food items → restore_stam_small / rest / buff_all_stats
  scrolls → skill_fireball / skill_lightning / skill_teleport / skill_resurrect
  bombs → proc_burn_aoe / proc_chill_aoe / proc_stun_aoe / proc_stealth
  traps → deploy_caltrops / deploy_trap_stun
Gold values added to all bombs, scrolls, buff potions, traps

merchant.js

Herbalist stock updated to clean tier ladder (minor/standard/major for all three lines)
Antidote and elixir added
Duplicate smoke bomb entries removed
Alchemist pool cleaned up

Outstanding / Notes

Consumable system design established: items fire real skills directly — no wrapper skills.
  The skill lands in the character's pool; children can discover off it via normal proc tree.
  Consumable charges decrement when a child procs. This is the intended mechanic.

use_* skills are gone. Do not re-introduce them.

stamina_potion_minor → restore_stam_minor is intentional. second_wind parents off
  restore_stam_minor + first_aid at 8% procChance. Do not change that item's skillID.

skill_teleport (utility: teleport_random) is non-functional — engine has no utility effect handler.
  Teleport scroll should trigger guaranteed retreat via the existing retreat clause.
  Wiring deferred — combat engine change needed, not data.

heal_major (the original combat skill, scalesBy harmony) is still used by mend's effect and
  health_potion/major items were intentionally given their own flat heal skills (heal_standard,
  heal_major2) to avoid harmony dependency on consumables. heal_major the combat skill is untouched.

Smoketest recommended — proc_stealth on smoke_bomb, purge on antidote, flat heal amounts
  on health potions. flat heal scalesBy field is new — confirm engine handles it.
  If engine only handles basePower/harmony/stat scaling, flat heals will silently do nothing
  and need a fixedHP field or similar instead.

One thing worth flagging before you close out: the scalesBy: flat field on the new heal skills is new convention — confirm the engine handles it before assuming the flat HP heals work. If it doesn't, next session will need a quick engine fix or a fixedHP field instead. Everything else should be solid.Claude handoffTXT DownloadLet's just fix that now to be sure.11:59 AMRan 2 commandsRan 2 commandsConfirmed — the engine computes restoreAmount = maxPoolValue * magnitude * scaleMultiplier. If scalesBy is flat it falls through to statValue = 0, making scaleMultiplier = 1, and then restoreAmount = maxHP * 15 * 1 — which would heal for 1500% of max HP. The flatAmount field is also ignored entirely.
Cleanest fix: handle scalesBy === 'flat' explicitly, using flatAmount directly as the restore amount.Ran 3 commands, edited 2 files, read a fileRan 3 commands, edited 2 files, read a fileSingle change in the heal resolver — scalesBy: flat now uses effect.flatAmount directly, bypassing the maxPool * magnitude * scaleMultiplier path entirely. Sudden death still applies on top for HP heals, which is correct — a potion mid-sudden-death should still be penalised.CombatengineJS Download
