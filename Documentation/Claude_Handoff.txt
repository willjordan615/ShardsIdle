Right, fixed. Looks good. Write up a handoff.9:35 PMSession Handoff
Delivered This Session
combatEngine.js

STAT_SCALE 300 → 200 — stat investment now meaningfully affects damage for all combatants
Enemy spawn-time stat scaling: 240 + 6*(level-1) budget, data stats define distribution shape
Procedural enemy weapon generation via generateEnemyWeapon() — replaces weaponTypes entirely. Tags drive damage type and style (fast/standard/heavy), level drives damage value on a smooth curve. weaponTypes field is now dead but still present in the data — can be stripped when convenient
Tuning constants extracted to generateEnemyWeapon.TUNING with hot-reload support via CombatEngine.applyTuning()
Taunt reworked: applied to enemies (not players), redirects to sourceId (the caster) via 4x weight in _weightedRandomTarget
Profile targeting completely overhauled — all profiles now run through _weightedRandomTarget on a preference-sorted candidate pool. Status weights (taunt, stealth, etc.) now apply universally regardless of enemy AI profile
spire_silent_acolyte weaponTypes fixed to ['dagger','sword'], spire_ward_keeper to ['hammer','mace'], vulture_wolf weaponTypes removed (creatureOnly jaws take over)

combat-log.js

DAMAGE_SINGLE base XP: 2.0 → 20.0
Skill XP bonus pool: 5% of xpGained distributed proportionally by usage count per skill
Skill level soft cap: XP awards reduced to 10% past level 20, levels continue indefinitely

database.js

getAllUsers() — lists all users with guest status
reassignCharacter(characterId, toUserId) — reassigns character ownership
reassignSnapshots(characterId, toUserId) — reassigns snapshot ownership

backend/routes/admin.js

GET /api/admin/db/users
POST /api/admin/db/characters/:id/reassign — accepts toUserId and includeSnapshots
GET /api/admin/tuning — reads tuning.json
POST /api/admin/tuning — writes tuning.json and hot-applies via CombatEngine.applyTuning()

backend/routes/combat.js

Loads tuning.json on engine init and applies via applyTuning()

js/admin-panel.js

Characters tab: Reassign button per character — modal with searchable user list, snapshot checkbox
Tuning tab: sliders for all 6 genWeapon constants, live damage preview table, Save & Apply

index.html

Tuning tab button and content div added to admin panel
admin-panel.js version bumped to v5

backend/data/tuning.json — new file, deploy to backend/data/

Outstanding / Notes

weaponTypes field still present on all enemies in enemy-types.json — dead code now, can be stripped in a future cleanup pass
Archbishop Malacor hitting himself with Blizzard — noted, not investigated
claws_small/large exist in items but no enemies assigned yet
Two-handed weapons / dual-wield — blocked on design decisions
Spear/polearm — blocked on two-handed
Enemy damage tuning still in progress — new procedural system gives much finer control via the Tuning tab; recommend a few smoketests across challenge levels to dial in BASE_PER_LVL and style multipliers
Version numbers on all changed files should be bumped in index.html